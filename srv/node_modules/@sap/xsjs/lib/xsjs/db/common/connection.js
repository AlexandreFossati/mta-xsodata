'use strict';

var hdbext = require('@sap/hdbext');

exports = module.exports;
exports.connect = connect;

function connect(hanaOptions, cb) {
  hdbext.createConnection(hanaOptions, function (err, client) {
    if (err) {
      return cb(err);
    }

    client.setAutoCommit(false);
    cb(null, client);
  });
}
